class ApplicationController < ActionController::Base

	# helper_method :current_cart
	
	# def current_cart
	# 	if session[:cart_id]
	# 		cuurent_cart = CartItem.find_by(id: session[:cart_id])
	# 	else
	# 		cuurent_cart = CartItem.create
	# 		session[:cart_id] = current_customer.id
	# 	end
	# end

end
