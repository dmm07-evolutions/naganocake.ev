<div class="container">
	<div class="row">
　　　<%= render 'shared/genre'%>
		 <div class="col-md-10 col-md-offset-3">
					<% unless genre_serch?　%>
						<h1>商品一覧(全件)</h1>
			            <div class="col-md-2">
			                　　<% @items.each do |item| %>
				                   <table>
				                     <tbody>
					                     <tr>
					                     	<td><%=attachment_image_tag(item.imag,fallback:"noimg.jpg",size:"50*50") %></td>
					                         <td><%= item.name %></td>
					                         <td><%= item.genre %></td>
					                   </tr>
				                      </tbody>
				                    </table>
				                <% end %>
				         </div>
						<h1>オススメ商品(全件)</h1>
			            <div class="col-md-2">
			                　　<% @items.each do |item| %>
				                   <table>
				                     <tbody>
					                     <tr>
					                     	<td><%=attachment_image_tag(item.imag,fallback:"noimg.jpg",size:"50*50") %></td>
					                         <td><%= item.name %></td>
					                         <td><%= item.genre %></td>
					                   </tr>
				                      </tbody>
				                    </table>
				                <% end %>
				         </div>
			        <% else %>
						<h1>ジャンル一覧(全件)</h1>
			            <div class="col-md-2">
			                　　<% @items.each do |item| %>
				                   <table>
				                     <tbody>
					                     <tr>
					                     	<td><%=attachment_image_tag(item.imag,fallback:"noimg.jpg",size:"50*50") %></td>
					                         <td><%= item.name %></td>
					                         <td><%= item.genre %></td>
					                   </tr>
				                      </tbody>
				                    </table>
				                <% end %>
						<h1>ジャンル商品(全件)</h1>
			            <div class="col-md-2">
			                　　<% @items.each do |item| %>
				                   <table>
				                     <tbody>
					                     <tr>
					                     	<td><%=attachment_image_tag(item.imag,fallback:"noimg.jpg",size:"50*50") %></td>
					                         <td><%= item.name %></td>
					                         <td><%= item.genre %></td>
					                   </tr>
				                      </tbody>
				                    </table>
				                <% end %>
				         </div>

          			<% end %>
            </div>
         </div>
	</div>
</div>