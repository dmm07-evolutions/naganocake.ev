	<div class="row">
		<h3>商品詳細</h3>
		<div class="col-md-4">
			<%= @item.image %>
		<div class="col-md-4">
			<p>商品名</p>
			<%= @item.name %>

			<p>商品説明</p>
			<%= @item.introduction %>

			<p>ジャンル</p>
			<%= @item.genre %>

			<p>税込価格（税抜価格）</p>
			<%= @item.price, {(@item.price*1.08)} %>

			<p>販売ステータス</p>
			<%= @item.status %>

<%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-primary col-md-3" %>

</div>
</div>
</div>